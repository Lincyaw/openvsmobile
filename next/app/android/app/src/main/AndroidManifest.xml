<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Camera is used only by the backend-pairing QR scanner. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- One-shot dictation for eyes-free plugin controls. Requested only
         when the user taps a voice-enabled field. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Foreground service for notification delivery (design §4.5). The
         service holds an always-on WS so backend notifications reach the
         system tray even when the app is backgrounded. POST_NOTIFICATIONS
         is Android 13+ runtime permission; FOREGROUND_SERVICE_* is the
         per-type declaration required on Android 14+. The plugin itself
         declares its base service in its own manifest; we add the
         permissions here. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Asks the user to whitelist the app from Doze / battery optimization
         so the foreground service's WebSocket isn't frozen while the app is
         backgrounded. On stock Android this is enough; on MIUI/EMUI the
         vendor's own "autostart" / "no restrictions" toggles must still be
         set by hand — there is no public API for those. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <!-- Allows receiving multicast packets on restrictive OEMs (Xiaomi,
         Huawei, Oppo). The mDNS discovery path needs this to hear LAN
         advertisements from the backend. Stock Android works without it,
         but the permission is harmless when unused. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>
    <!-- In-app self-update: allows the app to request the system package
         installer to sideload a downloaded APK. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <application
        android:label="MobileCode"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep link from ntfy notification tap. The notification ID
                 lands in the path; main.dart parses and routes to the
                 notification center via DeepLinkService. MainActivity's
                 launchMode="singleTop" ensures taps while the app is alive
                 deliver through onNewIntent instead of starting a fresh
                 instance. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="mobilecode" android:host="notifications"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <!-- Foreground service for background notification delivery.
             Must be declared explicitly; the plugin does not merge it. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- FileProvider for in-app APK updates. Shares the cache
             directory so the system package installer can read the
             downloaded APK via a content:// URI. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
